/**
 * Wire-protocol version for the controller heartbeat and enrollment messages.
 * Bumped only on a breaking change to the authenticated message contract.
 */
export const HEARTBEAT_PROTOCOL_VERSION = 1 as const;

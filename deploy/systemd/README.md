# deploy/systemd

Deferred to a later phase. Real host paths, identities, schedules, networks,
and unit/service definitions are **not** defined here yet and must never be
committed with deployment-specific values.
